# 🧪 Тестирование скрипта локально

Инструкция по тестированию всех функций скрипта перед деплоем на сервер.

---

## 📋 Подготовка

### 1. Убедитесь что .env настроен

```bash
# Проверьте наличие .env
cat .env

# Должно быть:
# TELEGRAM_BOT_TOKEN=ваш_токен
# TELEGRAM_CHAT_ID=ваш_chat_id
# CHECK_INTERVAL_MINUTES=5
# LOG_LEVEL=info
```

### 2. Установите зависимости

```bash
npm install
```

---

## 🧪 Тест 1: Проверка Telegram уведомлений

Проверим что бот может отправлять сообщения:

```bash
node test-notifications.js
```

**Что должно произойти:**
- ✅ В Telegram придет 4 сообщения:
  1. Стартовое сообщение
  2. Уведомление о появлении товара (с ссылкой)
  3. Уведомление об ошибке
  4. Сообщение об успешных тестах

**Если не работает:**
- Проверьте `TELEGRAM_BOT_TOKEN` в `.env`
- Проверьте `TELEGRAM_CHAT_ID` в `.env`
- Убедитесь что вы написали боту хотя бы одно сообщение

---

## 🔄 Тест 2: Полный цикл мониторинга

Запустим настоящую проверку товаров:

```bash
./test-full-cycle.sh
```

**Что происходит:**
1. Создается тестовый `state.json` где все товары недоступны
2. Запускается скрипт мониторинга
3. Скрипт проверяет реальные страницы Amazon
4. Если товар РЕАЛЬНО в наличии - придет уведомление в Telegram

**Ожидаемый результат:**
- ✅ Стартовое сообщение в Telegram
- ✅ Логи в консоли о проверке товаров
- ✅ Если товар в наличии - уведомление в Telegram
- ✅ Обновленный `state.json` с текущими состояниями

**Остановка:** Нажмите `Ctrl + C`

---

## 🎯 Тест 3: Симуляция появления товара

Ручная симуляция появления товара в наличии:

### Шаг 1: Создайте тестовое состояние

```bash
cat > state.json << 'EOF'
{
  "https://amzn.in/d/bHg6AfW": {
    "available": false,
    "lastChecked": "2026-02-01T08:00:00.000Z",
    "productName": "Rs.1000 Sony PlayStation Store Gift Card"
  }
}
EOF
```

### Шаг 2: Запустите скрипт

```bash
npm run build
npm start
```

### Шаг 3: Следите за логами

В консоли увидите:
```
[2026-02-01T10:00:00.000Z] [INFO] 🚀 Запуск скрипта мониторинга Amazon.in
[2026-02-01T10:00:00.000Z] [INFO] ✅ Telegram бот инициализирован
[2026-02-01T10:00:00.000Z] [INFO] Начало проверки 6 товаров...
[2026-02-01T10:00:05.000Z] [INFO] Товар "PlayStation Store Gift Card": ✅ В наличии
[2026-02-01T10:00:05.000Z] [INFO] 🎉 ТОВАР ПОЯВИЛСЯ В НАЛИЧИИ: PlayStation Store Gift Card
```

**Что проверяем:**
- ✅ Скрипт запускается без ошибок
- ✅ Проверка товаров работает
- ✅ Если товар был unavailable и стал available - придет уведомление
- ✅ state.json обновляется корректно

---

## 🐛 Тест 4: Проверка обработки ошибок

### Симуляция блокировки Amazon

Временно добавьте неправильный URL в `src/config.ts`:

```typescript
export const PRODUCT_URLS = [
  'https://amazon.in/invalid-url-test',  // Это вызовет ошибку
  'https://amzn.in/d/bHg6AfW',
];
```

Запустите:
```bash
npm run build
npm start
```

**Ожидаемое поведение:**
- ❌ Ошибка в логах для неправильного URL
- ✅ После 3 ошибок подряд - уведомление в Telegram
- ✅ Остальные товары проверяются нормально

**Не забудьте удалить тестовый URL после проверки!**

---

## 📊 Тест 5: Проверка интервала

Запустите скрипт и проверьте что он работает с заданным интервалом:

```bash
npm start
```

В `.env` установлен `CHECK_INTERVAL_MINUTES=5`, значит:
- ✅ Первая проверка сразу при запуске
- ✅ Вторая проверка через 5 минут
- ✅ Третья проверка через 10 минут
- И так далее...

В логах увидите:
```
[INFO] Следующая проверка через 5 минут
```

---

## ✅ Чеклист полного тестирования

Перед деплоем на сервер убедитесь:

- [ ] ✅ Telegram бот отправляет сообщения (`node test-notifications.js`)
- [ ] ✅ Скрипт запускается без ошибок (`npm start`)
- [ ] ✅ Проверка товаров работает (видны логи в консоли)
- [ ] ✅ Уведомления о появлении товара приходят в Telegram
- [ ] ✅ state.json создается и обновляется
- [ ] ✅ Скрипт корректно останавливается по Ctrl+C
- [ ] ✅ При повторном запуске состояние загружается из state.json

---

## 🎮 Быстрые команды для тестирования

```bash
# 1. Тест уведомлений
node test-notifications.js

# 2. Полный тест
./test-full-cycle.sh

# 3. Обычный запуск
npm run build && npm start

# 4. Запуск с логами debug
echo "LOG_LEVEL=debug" >> .env
npm start

# 5. Очистка тестовых данных
rm -f state.json
```

---

## 🔍 Что смотреть в логах

### Успешная работа:
```
[INFO] 🚀 Запуск скрипта мониторинга Amazon.in
[INFO] ✅ Telegram бот инициализирован
[INFO] ✅ Тестовое уведомление отправлено
[INFO] Начало проверки 6 товаров...
[INFO] Товар "PlayStation Store Gift Card": ✅ В наличии
[INFO] Проверка завершена: 1 из 6 товаров в наличии
[INFO] Следующая проверка через 5 минут
```

### Обнаружение ошибок:
```
[ERROR] Ошибка при запросе к https://... (попытка 1/3): timeout
[DEBUG] Ожидание 1000мс перед следующей попыткой...
[ERROR] Ошибка при запросе к https://... (попытка 2/3): timeout
```

### Уведомление о появлении товара:
```
[INFO] 🎉 ТОВАР ПОЯВИЛСЯ В НАЛИЧИИ: Rs.1000 Sony PlayStation Store Gift Card
[INFO] 📨 Отправлено уведомление: Rs.1000 Sony PlayStation Store Gift Card
```

---

## 🚨 Решение проблем

### Проблема: "TELEGRAM_BOT_TOKEN не установлен"

**Решение:**
```bash
cp .env.example .env
nano .env  # Добавьте токены
```

### Проблема: "Cannot find module 'node-telegram-bot-api'"

**Решение:**
```bash
npm install
```

### Проблема: "Permission denied: ./test-full-cycle.sh"

**Решение:**
```bash
chmod +x test-full-cycle.sh
```

### Проблема: Уведомления не приходят

**Решение:**
1. Проверьте что бот запущен: https://t.me/YOUR_BOT_USERNAME
2. Напишите боту любое сообщение
3. Проверьте токен: https://api.telegram.org/bot<TOKEN>/getMe
4. Проверьте chat_id: https://api.telegram.org/bot<TOKEN>/getUpdates

---

## 🎉 После успешного тестирования

Если все тесты прошли успешно, можно деплоить на сервер:

```bash
# На вашем Mac
git add .
git commit -m "test: все тесты пройдены, готово к деплою"
git push

# На сервере
ssh your_user@your_server_ip
cd ~/amazon-checker
./update.sh
```

Готово! 🚀
