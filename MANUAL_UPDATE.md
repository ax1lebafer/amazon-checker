# 🔄 Ручное обновление на сервере

Простая инструкция как обновить код на сервере после изменений.

---

## 📦 Быстрое обновление (один скрипт)

```bash
# Подключитесь к серверу
ssh your_user@your_server_ip

# Запустите скрипт обновления
cd ~/amazon-checker
./update.sh
```

Готово! ✅

---

## 🔧 Ручное обновление (пошагово)

Если хотите обновить вручную:

```bash
# 1. Подключитесь к серверу
ssh your_user@your_server_ip

# 2. Перейдите в директорию проекта
cd ~/amazon-checker

# 3. Получите последние изменения из GitHub
git pull origin main

# 4. Установите/обновите зависимости (если изменился package.json)
npm install

# 5. Скомпилируйте TypeScript
npm run build

# 6. Перезапустите сервис
sudo systemctl restart amazon-monitor

# 7. Проверьте статус
sudo systemctl status amazon-monitor
```

---

## 📋 Полезные команды

### Просмотр логов в реальном времени:
```bash
sudo journalctl -u amazon-monitor -f
```

### Просмотр последних 50 строк логов:
```bash
sudo journalctl -u amazon-monitor -n 50
```

### Остановка сервиса:
```bash
sudo systemctl stop amazon-monitor
```

### Запуск сервиса:
```bash
sudo systemctl start amazon-monitor
```

### Перезапуск сервиса:
```bash
sudo systemctl restart amazon-monitor
```

### Проверка статуса:
```bash
sudo systemctl status amazon-monitor
```

---

## 🎯 Когда нужно обновлять?

Обновляйте код на сервере когда:
- ✅ Изменили код локально и сделали `git push`
- ✅ Добавили новые товары для мониторинга
- ✅ Изменили интервал проверки
- ✅ Исправили баги

---

## ⚡ Быстрая шпаргалка

```bash
# Одной командой (рекомендуется)
ssh your_user@your_server_ip "cd ~/amazon-checker && ./update.sh"

# Или подключитесь и выполните:
ssh your_user@your_server_ip
cd ~/amazon-checker && git pull && npm run build && sudo systemctl restart amazon-monitor
```

---

## 🐛 Решение проблем

### Проблема: Git pull требует пароль

**Решение:**
```bash
cd ~/amazon-checker
git config credential.helper store
git pull  # Введите логин/пароль GitHub один раз
```

### Проблема: npm install не работает

**Решение:**
```bash
# Обновите Node.js
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt-get install -y nodejs

# Попробуйте снова
npm install
```

### Проблема: Permission denied при компиляции

**Решение:**
```bash
# Установите права на директорию
sudo chown -R $USER:$USER ~/amazon-checker
chmod -R 755 ~/amazon-checker
```

### Проблема: Сервис не перезапускается

**Решение:**
```bash
# Проверьте логи ошибок
sudo journalctl -u amazon-monitor -n 50 --no-pager

# Остановите и запустите заново
sudo systemctl stop amazon-monitor
sudo systemctl start amazon-monitor
```

---

## ✅ Проверка что обновление прошло успешно

После обновления проверьте:

```bash
# 1. Посмотрите последний коммит
git log -1 --oneline

# 2. Проверьте что сервис работает
sudo systemctl status amazon-monitor

# 3. Посмотрите свежие логи
sudo journalctl -u amazon-monitor -n 20
```

Должны увидеть:
- ✅ Последний коммит совпадает с GitHub
- ✅ Сервис в статусе `active (running)`
- ✅ В логах нет ошибок

---

## 💡 Полезные алиасы для быстрого доступа

Добавьте в `~/.bashrc` на сервере для быстрых команд:

```bash
# Откройте файл
nano ~/.bashrc

# Добавьте в конец:
alias am-update='cd ~/amazon-checker && ./update.sh'
alias am-logs='sudo journalctl -u amazon-monitor -f'
alias am-status='sudo systemctl status amazon-monitor'
alias am-restart='sudo systemctl restart amazon-monitor'

# Сохраните и примените
source ~/.bashrc
```

Теперь можно использовать короткие команды:
- `am-update` - обновить код
- `am-logs` - смотреть логи
- `am-status` - статус сервиса
- `am-restart` - перезапуск

---

Готово! Теперь обновление занимает буквально 10 секунд. 🚀
