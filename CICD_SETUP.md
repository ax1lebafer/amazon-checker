# 🚀 Настройка автоматического деплоя (CI/CD)

Этот гайд поможет настроить автоматический деплой на сервер при каждом пуше в `main` ветку.

## 📋 Что нужно

1. Доступ к вашему серверу по SSH
2. GitHub репозиторий (уже есть ✅)
3. 5-10 минут для настройки

---

## 🔑 Шаг 1: Создание SSH ключа для GitHub Actions

На **вашем компьютере** (или на сервере):

```bash
# Создаём новый SSH ключ специально для GitHub Actions
ssh-keygen -t ed25519 -C "github-actions" -f ~/.ssh/github_actions_key -N ""

# Выводим приватный ключ (НЕ ДЕЛИТЕСЬ ИМ!)
cat ~/.ssh/github_actions_key

# Выводим публичный ключ
cat ~/.ssh/github_actions_key.pub
```

**Сохраните оба ключа** - они понадобятся на следующих шагах.

---

## 🖥️ Шаг 2: Настройка сервера

### 2.1 Добавьте публичный ключ на сервер

Подключитесь к серверу и выполните:

```bash
# Подключаемся к серверу
ssh your_user@your_server_ip

# Добавляем публичный ключ
echo "ВСТАВЬТЕ_СЮДА_ПУБЛИЧНЫЙ_КЛЮЧ" >> ~/.ssh/authorized_keys

# Устанавливаем правильные права
chmod 600 ~/.ssh/authorized_keys
```

### 2.2 Клонируйте репозиторий на сервер

```bash
# Если еще не клонировали
cd ~
git clone https://github.com/ax1lebafer/amazon-checker.git
cd amazon-checker

# Настройте .env
cp .env.example .env
nano .env  # Добавьте токены

# Запустите деплой скрипт
chmod +x deploy.sh
./deploy.sh
```

### 2.3 Настройте Git для pull без пароля

```bash
cd ~/amazon-checker
git config pull.rebase false
```

### 2.4 Разрешите пользователю перезапускать сервис без sudo пароля

```bash
# Создаём файл sudoers для amazon-monitor
sudo visudo -f /etc/sudoers.d/amazon-monitor
```

Добавьте эту строку (замените `your_username` на ваше имя пользователя):

```
your_username ALL=(ALL) NOPASSWD: /bin/systemctl restart amazon-monitor, /bin/systemctl status amazon-monitor
```

Сохраните и выйдите (Ctrl+X, Y, Enter).

Проверьте:
```bash
sudo systemctl restart amazon-monitor
# Не должно просить пароль
```

---

## 🔐 Шаг 3: Настройка GitHub Secrets

1. Откройте ваш репозиторий на GitHub: https://github.com/ax1lebafer/amazon-checker

2. Перейдите в **Settings** → **Secrets and variables** → **Actions**

3. Нажмите **New repository secret** и добавьте следующие секреты:

### Секрет 1: `SSH_PRIVATE_KEY`
- **Name:** `SSH_PRIVATE_KEY`
- **Value:** Вставьте содержимое приватного ключа (`cat ~/.ssh/github_actions_key`)
- Нажмите **Add secret**

### Секрет 2: `SERVER_HOST`
- **Name:** `SERVER_HOST`
- **Value:** IP адрес вашего сервера (например: `192.168.1.100`)
- Нажмите **Add secret**

### Секрет 3: `SERVER_USER`
- **Name:** `SERVER_USER`
- **Value:** Имя пользователя на сервере (например: `ubuntu`)
- Нажмите **Add secret**

### Секрет 4: `SERVER_PORT` (опционально)
- **Name:** `SERVER_PORT`
- **Value:** `22` (или другой SSH порт, если изменён)
- Нажмите **Add secret**

---

## ✅ Шаг 4: Проверка работы

1. **Сделайте тестовое изменение:**

```bash
# На вашем компьютере
cd /Users/ax1lebafer/Desktop/Repository/check-stuff

# Измените что-нибудь, например:
echo "# Test Auto-Deploy" >> README.md

# Закоммитьте и запушьте
git add .
git commit -m "test: проверка автодеплоя"
git push
```

2. **Проверьте GitHub Actions:**
   - Откройте: https://github.com/ax1lebafer/amazon-checker/actions
   - Вы увидите запущенный workflow "Deploy to Server"
   - Дождитесь завершения (обычно 1-2 минуты)

3. **Проверьте на сервере:**

```bash
ssh your_user@your_server_ip
cd ~/amazon-checker
git log -1  # Должен показать ваш последний коммит
sudo journalctl -u amazon-monitor -n 20  # Логи после перезапуска
```

---

## 🎯 Что происходит автоматически

При каждом `git push` в ветку `main`:

1. ✅ GitHub Actions запускается автоматически
2. 📦 Подключается к вашему серверу по SSH
3. 🔄 Делает `git pull` для получения нового кода
4. 📥 Устанавливает новые зависимости (если есть)
5. 🔨 Компилирует TypeScript
6. 🔄 Перезапускает systemd сервис
7. ✅ Проверяет что сервис запустился

Весь процесс занимает **1-2 минуты**.

---

## 🔧 Ручное обновление (без GitHub Actions)

Если нужно обновить код вручную на сервере:

```bash
ssh your_user@your_server_ip
cd ~/amazon-checker
chmod +x update.sh
./update.sh
```

---

## 🐛 Решение проблем

### Проблема: GitHub Actions не может подключиться к серверу

**Решение:**
1. Проверьте что публичный ключ добавлен на сервер
2. Проверьте правильность `SERVER_HOST`, `SERVER_USER`, `SERVER_PORT`
3. Убедитесь что SSH ключ скопирован полностью (включая начало и конец)

### Проблема: Permission denied при перезапуске сервиса

**Решение:**
Проверьте что sudoers файл настроен правильно:

```bash
sudo cat /etc/sudoers.d/amazon-monitor
# Должна быть строка с NOPASSWD
```

### Проблема: Git pull требует авторизацию

**Решение:**
```bash
cd ~/amazon-checker
git config credential.helper store
git pull  # Введите логин/пароль один раз
```

Или используйте Personal Access Token:
```bash
git remote set-url origin https://TOKEN@github.com/ax1lebafer/amazon-checker.git
```

---

## 📊 Мониторинг деплоев

### Просмотр истории деплоев:
https://github.com/ax1lebafer/amazon-checker/actions

### Просмотр логов на сервере:
```bash
sudo journalctl -u amazon-monitor -f
```

### Отмена деплоя:
Если что-то пошло не так, откатитесь к предыдущему коммиту:

```bash
ssh your_user@your_server_ip
cd ~/amazon-checker
git log --oneline -5  # Смотрим последние коммиты
git reset --hard COMMIT_HASH  # Откатываемся
npm run build
sudo systemctl restart amazon-monitor
```

---

## 🎉 Готово!

Теперь каждый раз когда вы делаете `git push`, код автоматически обновится на сервере! 🚀

## 💡 Дополнительные улучшения

Можно добавить:
- ✉️ Уведомления в Telegram при успешном/неудачном деплое
- 🧪 Автоматические тесты перед деплоем
- 🔄 Rollback при ошибках
- 📊 Slack/Discord уведомления

Нужна помощь с настройкой? Напишите!
